# Development Dockerfile for frontend
# Build context should be project root to include shared folder
FROM node:18-alpine

# Install wget for healthcheck
RUN apk add --no-cache wget

WORKDIR /app

# Copy shared folder to parent directory (frontend imports from ../../../../shared/)
COPY shared/ /shared/

# Copy frontend package files
COPY frontend/package*.json ./

# Install all dependencies
RUN npm ci

# Copy frontend source code
COPY frontend/ ./

# Expose Vite development port
EXPOSE 5173

# Run Vite in development mode with host binding
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
